package git.david;

public class Config {

    public static int draw_sleep_time = 40; // Default: 40 | Sleep time between re-drawing the screen lower values make the game run faster
    public static boolean hints_visibility = true;
}
